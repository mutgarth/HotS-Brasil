<%= form_with(model: build, local: true) do |form| %>
  <% if build.errors.any? %>

    <div id="error_explanation">
      <h2><%= pluralize(build.errors.count, "error") %> prohibited this build from being saved:</h2>

      <ul>
      <% build.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container text-center">
    <%= collection_select(:character, :character_id, Character.all, :id, :name_with_initial, prompt: true) %>

    <div class="field">
      <%= form.label :title %>
      <%= form.text_field :title, id: :build_title %>
    </div>

    <div class="field">
      <%= form.label :description %>
      <%= form.text_area :description, id: :build_description %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
    <% end %>

  </div>
